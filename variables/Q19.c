#include<stdio.h>
int main()
{
    int units,custId,amount;
    float surcharge=0,payable;
    printf("entre the cust id :");
    scanf("%d",&custId);
    printf("entre the unit consumed :");
    scanf("%d",&units);

    if (units<200 && units>0
    {
        
    })
    return 0;
}